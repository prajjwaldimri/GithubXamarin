<UserControl
    x:Class="GithubXamarin.UWP.UserControls.IconAndTextBlocksControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GithubXamarin.UWP.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="10*"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>

        <FontIcon FontFamily="ms-appx:/Assets/Fonts/ionicons.ttf#Ionicons"
                                   Glyph="{Binding FontIconGlyph, ElementName=UserControl}"
                                  Grid.Row="0" Grid.Column="0"/>
        <TextBlock Text="{Binding MiddleTextBlockProperty, ElementName=UserControl}" FontSize="18" Margin="10,0,0,0"
                                   Grid.Row="0" Grid.Column="1"/>
        <TextBlock Text="{Binding LastTextBlockProperty, ElementName=UserControl}" FontSize="18" Margin="10,0,0,0"
                                   Grid.Row="0" Grid.Column="2"/>

    </Grid>
</UserControl>
